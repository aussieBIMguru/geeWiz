<Window x:Class="geeWiz.Forms.ProgressView"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:local="clr-namespace:geeWiz.Forms"
        mc:Ignorable="d">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- For the Label -->
            <RowDefinition Height="Auto" />
            <!-- For the ProgressBar -->
        </Grid.RowDefinitions>

        <Label x:Name="progressLabel" Content="{Binding ProgressText}" HorizontalAlignment="Center" Margin="0,0,0,5" Grid.Row="0"/>
        <ProgressBar x:Name="progressBar" Height="15" Width="400" Grid.Row="1" Foreground="DarkCyan" Background="LightGray"/>
    </Grid>
</Window>